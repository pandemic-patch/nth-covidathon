const initState = {
  signedIn: false,
  remoteDataLoaded: false,
};

const patientReducers = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default patientReducers;
